version: "3.9"

services:
  misho:
    image: misho
    build:
      context: .
      dockerfile: Dockerfile
      args:
        MISHO_MAIL_USERNAME: ${MISHO_MAIL_USERNAME}
        MISHO_MAIL_PASSWORD: ${MISHO_MAIL_PASSWORD}
        MISHO_ENVIRONMENT: ${MISHO_ENVIRONMENT}
    volumes:
      - ./db:/root/db
    ports:
      - "8000:8000"
