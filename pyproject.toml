[project]
name = "misho"
version = "0.1.0"
dependencies = [
    "nodejs>=0.1.1",
    "paramiko>=3.5.1",
    "pyright>=1.1.403",
    "scp>=0.15.0",
]

[tool.uv.workspace]
members = [
    "sportbooking",
    "misho-server",
]
