[project]
name = "misho"
version = "0.1.0"
dependencies = [
    "paramiko>=3.5.1",
    "scp>=0.15.0",
]

[tool.uv.workspace]
members = [
    "misho-api",
    "misho-client",
    "misho-cli",
    "sportbooking",
    "misho-server",
]
